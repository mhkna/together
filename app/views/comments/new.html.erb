<% provide(:title, "Account") %>

<div class="page-wrapper comment-container">
  <div class="box-heading">
    <p>Step 2 of 2 (optional)</p>
  </div>

    <h3 class="page-heading">Suggest <%= 'Comment'.pluralize(@account.match_amount) %></h3>

    <%= render 'comments/form' %>
</div>
