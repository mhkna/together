<%= form_with(model: [ @account, @account.comments.build ], local: true) do |form| %>
  <div>
    <%= form.text_field :text, autofocus: true, placeholder: 'Comment' %>
  </div>

  <div>
    <%= form.submit "Join Round" %>
  </div>
<% end %>
