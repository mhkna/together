<%= form_with(model: @comment, url: account_comment_path, local: true) do |form| %>
  <div>
    <%= form.text_field :text, autofocus: true, placeholder: 'Comment' %>
  </div>

  <div>
    <%= form.submit "Save" %>
  </div>
<% end %>
