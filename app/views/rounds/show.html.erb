<p>
  Like and comment on the latest post by the user below.
</p>

<h3><%= @round.accounts.first.website.capitalize %></h3>

<div>
  <div>Username:</div>
  <strong><%= @round.account_slice(5).username %></strong>
</div>

<% if @round.account_slice(5).comments.any? %>
  <div>
    <div>Suggested comment:</div>
    <strong><%= @round.account_slice(5).comments.first.try(:text) %></strong>
  </div>
<% end %>
